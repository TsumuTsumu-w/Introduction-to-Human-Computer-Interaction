# frontend

Vue 3 + Vite 前端。页面已经按可扩展平台结构拆分，后续小游戏模块建议只新增到 `src/modules` 与 `src/views/modules`，不要重做外层壳子。

运行：

```powershell
cd frontend
npm install
npm run dev
```

构建检查：

```powershell
npm run build
```
