# login-park assets

This folder contains the local visual assets used by the GestureFlow login page.
The current main background is `scene/park-entrance-approved.png`.
The real Vue input fields, login button, default-ticket button, and reserved register button are overlaid in `src/views/LoginView.vue`.
